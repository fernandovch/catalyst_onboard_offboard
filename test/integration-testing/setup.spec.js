before("Setting up test cases", function () {
    console.info(
        "------------------- Starting test execution --------------------"
    );
});

after("Finalizing test cases", function () {
    console.info(
        "----------------------- Test finalized -------------------------"
    );
});
